"""Known per-site defaults for seeding and migrations."""

from __future__ import annotations

# Main fuse (A) — used when seeding new sites and documented for ops.
DEFAULT_MAIN_FUSE_A: dict[str, float] = {
    "summer-house-denmark": 50.0,
}
