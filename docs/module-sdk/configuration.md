# Configuration

Configuration schema follows the same field model as built-in modules. Site `config_json` is preserved on compatible updates; major migrations may use `migrations/` scripts with backup snapshots.
